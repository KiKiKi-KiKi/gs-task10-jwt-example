version: '3.1'
services:
  redis:
    image: redis:latest
    volumes:
      - ./redis/data:/data
    ports:
      - 6379:6379

  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ./mongodb/data:/data/db
    restart: always


  api:
    build:
      context: './api'
      dockerfile: 'Dockerfile'
    environment:
      - MONGO_DATABASE=mongodb://mongodb:27017/MyApp
    volumes:
      - ./api:/api
      - ./api/package.json:/api/package.json
      - ./api/package-lock.json:/api/package-lock.json
      - ./api/node_modules:/api/node_modules
    ports:
      - 3000:3000
    depends_on:
      - redis
    tty: true
    working_dir: "/api"
    command: bash -c "yarn start"
